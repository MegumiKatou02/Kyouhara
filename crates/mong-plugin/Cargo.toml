[package]
name = "mong-plugin"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Không bật "sync": cả desktop lẫn web đều gọi host từ một luồng, Rc là đủ.
rhai = { version = "1", features = ["serde"] }
serde_json = "1"
