[package]
name = "mong-plugin"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Không bật "sync": cả desktop lẫn web đều gọi host từ một luồng, Rc là đủ.
rhai = { version = "1", default-features = false, features = ["serde", "no_time"] }
serde_json = "1"
